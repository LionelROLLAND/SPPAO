#include <iostream>
#include <list>
#include "Node.hpp"

using namespace std;


list<Node*>* makeGraph(int P, int Q) {
    list<Node*>* nodes = new list<Node*>();
    for (int i = 1; i <= P; i++) {
        for (int j = 1; j <= Q; j++) {
            
        }
    }
    return nodes;
}